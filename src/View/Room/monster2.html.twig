{% block content %}
    <div class="card card-display">
        <div class="card-body">
            <h5 class="card-title">{{ session.monster2.name }}</h5>
            <p class="card-text" >{{ session.monster2.description }}</p>
        </div>
        <ul class="list-group list-group-flush">
            <li class="list-group-item">Niveau : {{ session.monster2.level }}</li>
            <li class="list-group-item">Attaque : {{ session.monster2.attack }}</li>
            <li class="list-group-item">Defense : {{ session.monster2.defense }}</li>
        </ul>
    </div>
{% endblock %}